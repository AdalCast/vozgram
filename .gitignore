node_modules/
dist/
*.log
.DS_Store

# Secretos: NUNCA se publican
.env*
# ...pero la plantilla SI, es lo que necesita quien clone el repo
!.env.example

# Paquete construido: contiene el APP_SECRET inlineado por Vite
*.ehpk

# Credenciales de sesion de WhatsApp y base de mensajes.
# Quien tenga wa-auth puede leer y escribir por vos en WhatsApp: NUNCA se sube.
data/
*.db
*.db-wal
*.db-shm
